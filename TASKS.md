# TASKS.md

## Сейчас

### Видео в блоке «Как это устроено» (2026-09-21)

- [x] Добавлено self-hosted видео между заголовком и текстом блока «Как это устроено» — тизер 39 сек. Обсудили варианты (YouTube/VK/RuTube/self-hosted), решили в пользу self-hosted: ролик короткий, риска веса почти нет, а self-hosted полностью убирает риск рекламы перед оплатой и сторонних скриптов (см. переписку в этом же заходе). Затронуто: `src/project.js` (`howItWorks.video`/`howItWorks.videoPoster`), `src/app.js` (разметка `.video-frame`), `src/styles.css` (новые классы — см. `DESIGN_SYSTEM.md`).
- [x] Видео сжато локально через ffmpeg (переносимый бинарник через `npx`/npm-пакет `ffmpeg-static`, т.к. в системе не было ни ffmpeg, ни Homebrew) — с 26 МБ (HEVC 1234×1080) до 2 МБ (H.264 720×630, весь блок на странице всё равно не шире ~480px). Исходный сырой файл не хранится в репозитории — тот же принцип, что и с `about.webp`.
- [x] Оформлено в рамку `.video-frame` с тремя цветными кружками (красный/жёлтый/зелёный) в углу, имитирует окно приложения — по явной просьбе пользователя. Видео не грузится вместе со страницей (`preload="none"`, грузится только по нажатию play) — только лёгкий постер-превью (31 КБ, webp).
- [x] **Замена видео 2026-09-21 (той же датой):** пользователь по ошибке дал не тот файл — старый ролик где-то показывал ссылку, которую могли бы забрать посторонние. Заменено на исправленный файл, тем же способом сжато и вставлено. Старая (небезопасная) версия нигде не сохранилась — ни разу не коммитилась в git до замены, так что утечки не произошло.
- [x] Скрыта кнопка «скачать» в панели плеера — `controlsList="nodownload"` на `<video>`. Важно: это не защита, а просто косметика для случайного зрителя — сама видеоссылка всё равно публична (статический сайт без сервера/авторизации), при желании файл можно скачать напрямую по ссылке. Пользователь предупреждён и осознанно выбрал не усложнять архитектуру ради настоящей защиты.
- Проверено: `npm run check` на каждом шаге; Playwright — видео не грузится при обычном заходе на страницу (только постер), реально запускается и грузится по `play()`, `controlsList="nodownload"` применяется, горизонтальный скролл не изменился. Другие блоки не трогали.

### Путь после оплаты в блоке «Цена и доступ» — вертикальный степпер (2026-09-21)

- [x] Заменён абзац текста («Жмёшь кнопку — попадаешь в Телеграм...») на наглядную карту из 5 шагов: жмёшь кнопку → оплачиваешь удобным способом → попадаешь на страницу после оплаты → жмёшь «Забрать таблицы» → попадаешь в закрытый Telegram-канал. Затронуто: `src/project.js` (поле `final.text` → `final.steps`, массив из 5 строк), `src/app.js` (рендер `.step-path`/`.step-row`/`.step-marker` вместо `<p class="lead">`), `src/styles.css` (новые классы — см. `DESIGN_SYSTEM.md`). Шаги получили теги ячеек `F2…F6` (продолжают тег блока `F1`), соединены вертикальной линией.
- Не трогали: остальные блоки, цену, заголовок, кнопку — только сам список пути.
- Проверено: `npm run check`; Playwright на 390/1440px — 5 шагов с верным текстом и тегами, горизонтальный скролл не изменился (та же старая величина, не связана с этой правкой). Пользователь смотрел локальный превью на своей машине перед сохранением.

### Жирное выделение в блоке «Как это устроено» (2026-09-21)

- [x] Выделены жирным две фразы: «лежат в закрытом Telegram-канале» и «доступ к каналу у тебя будет навсегда, поэтому всегда сможешь вернуться.». Затронуто: `src/project.js` (поле `howItWorks.text` → `howItWorks.textHtml`, по тому же принципу, что и `benefit.textHtml` — HTML-разметка вместо `escapeHtml`), `src/app.js` (рендер без `escapeHtml`). Остальной текст и структура блока не менялись. Проверено: `npm run check`, Playwright — ровно эти два `<strong>`, ничего больше.

### Новый блок «Вот в чём твоя выгода» (2026-09-21)

- [x] Добавлен блок между «Коротко обо мне» и «Как это устроено» — новый порядок: оффер → что внутри → обо мне → **вот в чём твоя выгода** → как это устроено → цена и доступ. Затронуто: `src/project.js` (новое поле `benefit`: `title` + `textHtml` с `<strong>` на ключевых тезисах — единственное место в проекте, где текст рендерится как HTML, а не через `escapeHtml`, т.к. нужна разметка полужирным), `src/app.js` (новая `<section>`, деструктуризация `benefit`), `src/styles.css` (новый класс `.benefit-card` — см. `DESIGN_SYSTEM.md`).
- [x] Блок оформлен как «выделенный диапазон» в Google Таблицах: акцентная рамка (`border: 2px solid var(--accent)`) + более плотное свечение в фоне, чем у остальных `.block--glow-N`. Пробовали добавить квадратик-хэндл в углу (маркер заполнения, как в Sheets) — визуально мешал, по просьбе убрали, оставили только рамку и свечение. Кнопка «Забрать набор» с реальной ссылкой на оплату (`project.ctaUrl`) добавлена и в этот блок.
- [x] Буквенные теги ячеек сдвинуты, чтобы нумерация осталась сквозной: новый блок — `D1`, «Как это устроено» — `D1` → `E1`, «Цена и доступ» — `E1` → `F1`. Другие блоки (структура, стили, текст) не трогали.
- Проверено: `npm run check` проходит на каждом шаге; Playwright на 390/1440px — порядок блоков и теги верные, кнопка ведёт на `ctaUrl`, `<strong>` рендерится (4 шт.), горизонтальный скролл и 1 ошибка в консоли (`ERR_CERT_AUTHORITY_INVALID`) — сверены с версией без правки, те же самые, к задаче не относятся, не трогали. Пользователь смотрел локальный превью (`npm run dev` + `open`) на своей машине перед сохранением.

### План по итогам тестов на живых людях (2026-09-21)

- [x] Свайп-тумблер в hero лагал у одного из трёх тестировщиков. Сделано 2026-09-21: позиция ползунка (`.swipe-cta-thumb`) переключена с `left` на `transform: translateX()` в `src/app.js`/`src/styles.css` — `left` заставляет браузер пересчитывать вёрстку на каждое движение пальца, `transform` — только перерисовку слоя, дешевле для слабых телефонов. Заливка (`.swipe-cta-fill`) намеренно оставлена на `width` — со scaleX-трансформом у нее исказились бы круглые края пилюли, а конкретно на неё пользователь не жаловался. Попутно найден и исправлен риск: после переключения на transform старый код в двух местах читал текущую позицию через `thumb.offsetLeft` (переставало работать корректно, т.к. offsetLeft не учитывает transform) — заменено на отдельную JS-переменную `currentLeft`, которая всегда синхронна с реальной позицией. Проверено: клик по пилюле и Enter — уходят на оплату; протяг за порог (82% пути) и до него — так же, отдельно проверены оба сценария (завершение/сброс) точечной симуляцией pointer-событий, т.к. это окружение не эмулирует настоящий mouse-drag для этого компонента (не связано с правкой); скриншот тумблера — визуально не отличим от исходного; ошибок в консоли нет на 390px и 1440px; `npm run check` проходит.
- [x] Убрать блюр с картинок таблиц. Сделано 2026-09-21: `.table-card img` в `src/styles.css` — убран `blur(2px)` и связанный с ним `transform: scale(1.02)` (стоял только чтобы прятать размытые края, без блюра не нужен), `saturate(1.05)` оставлен. Затронуты все 5 картинок (3 видимые + 2 скрытые под «показать все 11 таблиц») — правило общее для всех. `DESIGN_SYSTEM.md` обновлён — блюр как приём для превью больше не описывается. Важно: первая версия этой правки была проверена только локально и не была отправлена на сайт — пользователь проверял живой адрес и закономерно не увидел изменений; отправлено в этом же заходе.
- [ ] Добавить контакт для связи на сайт (формат и место на странице — уточнить у пользователя).

- [x] Тест оплаты на тестовой ссылке (15 ₽) проведён пользователем 2026-09-21. `project.ctaUrl` возвращён на боевую ссылку `https://tochkaplace.com/ia/1189ff73-20e4-4d73-b885-0443fd393b3c` (2 490 ₽), подтверждено на нескольких браузерах/устройствах.
  - Важное наблюдение на будущее: после отката пользователь какое-то время видел на десктопе старую (тестовую) ссылку, хотя сервер уже отдавал верную — причина в кэше конкретного браузера (GitHub Pages ставит `cache-control: max-age=600`, плюс это одностраничное приложение — уже открытая вкладка не перезагружает код сама). Проверено `curl` напрямую с сервера — там всё было верно с самого начала. Решается жёстким обновлением (`Cmd+Shift+R`), приватным окном или очисткой кэша браузера. Держать в голове при следующих тестах на живых людях — предупреждать тестировщиков открывать сайт в приватном окне или заново, если что-то выглядит «старым».

- [x] Аудит доступности главного пути (390/768/1440px) и правка критичных находок
  - Пользователь: попросил проверить горизонтальный скролл, обрезанный текст, размер/доступность кнопок, порядок Tab и видимый фокус, подписи полей, контраст, смысл только цветом, увеличение текста до 200%.
  - Найдено и исправлено 5 критичных пунктов: (1) горизонтальный скролл на всех ширинах из-за псевдоэлементов «дыма» — добавлен `overflow-x:hidden` на `body`; (2) невидимый фокус на ползунке hero-CTA — кольцо перенесено на контейнер `.swipe-cta` через `:has()`, т.к. `overflow:hidden` на контейнере обрезал outline потомка; (3) skip-link был невидим даже в фокусе — добавлен класс `.skip-link` с правилом `:focus`; (4) drag-ползунок не реагировал на обычный клик/тап — добавлена click-альтернатива в `initHeroSwipe` (`src/app.js`), с защитой от гонки между `pointerup` и нативным `click` через `suppressNextClick`; (5) названия таблиц обрезались при zoom 200% на 390px — `.table-card-title` получил `min-width:0` и `overflow-wrap:anywhere`.
  - Затронуто: `src/styles.css` (`body`, `.swipe-cta-thumb:focus-visible`/`.swipe-cta:has(...)`, `.skip-link:focus`, `.table-card-title`), `src/app.js` (`initHeroSwipe`), `index.html` (класс `skip-link` на ссылке).
  - Не трогаем: остальные пункты отчёта помечены «важно»/«позже» (форма — неприменимо, полей ввода на лендинге нет; общий риск `.block{overflow:hidden}` для будущего длинного текста — держать в голове, не чинить превентивно).
  - Проверено: Playwright на 390/768/1440px — `window.scrollTo` больше не двигает страницу; фокус-кольцо на hero-CTA видно и не обрезано ни на одной ширине; skip-link виден и ведёт на `#main`; клик по пилюле (по кружку и по надписи) работает, полный/оборванный drag не сломан; заголовки таблиц при zoom 200% не обрезаются и не ломают вид при 100%; `npm run check` проходит после каждого шага.

- [x] Финальный блок — прямой оффер вместо формы заявки
  - Пользователь: вместо формы «оставь контакт» финальный блок сразу показывает цену (2 490 ₽) и объясняет путь («жмёшь кнопку → Telegram → оплата → бот добавляет в канал»). Кнопка ведёт на `project.ctaUrl` (заглушка `#`, пока нет реальной ссылки).
  - Затронуто: `src/project.js` (поле `order` удалено, `final` дополнено `price`/`priceNote`/`text`), `src/app.js` (удалены `renderOrderBody`/`initOrderSection`, блок 5 переписан), `src/styles.css` (`.price-line`).
  - Не трогаем: `project.ctaUrl` остаётся заглушкой — реальную ссылку впишем, когда она появится (см. «Позже»).
  - Проверено: `npm run check` проходит, форма и локальная запись `order_intent` нигде в коде не остались (проверено `grep`), кнопка в финальном блоке читает `ctaUrl`.

- [x] Единая точка для ссылки «Забрать набор»
  - Пользователь: все кнопки ведут в одно и то же место (пока заглушка) вместо разрозненных `href="#"`.
  - Затронуто: `src/project.js` (новое поле `ctaUrl`), `src/app.js` (кнопки читают его вместо хардкода).
  - Проверка руками: открыть сайт, нажать все кнопки «Забрать набор» по очереди — ведут на один и тот же адрес; значение меняется в одном месте в `project.js`.
  - Не трогаем: саму ссылку на Tribute (её ещё нет), шапку/навигацию, страницу «Заявки», текст лендинга.
  - Проверено: в коде найдено 3 кнопки (в задаче было указано 5) — все читают `project.ctaUrl`, автоматически проверено скриншотами в браузере (десктоп и мобильная ширина), `npm run check` проходит.

- [x] Главный путь на локальных данных (репетиция заявки вместо оплаты)
  - Пользователь: кнопка «Забрать набор» ведёт на блок `#order` с формой (контакт) — сабмит создаёт локальную запись `order_intent` через `store.js`, без базы/входа/оплаты.
  - Затронуто: `src/project.js` (тексты `order`), `src/app.js` (`renderOrderBody`, `initOrderSection`, кнопки-якоря `#order`).
  - Проверка руками (Playwright, 390px): пустая форма → короткое значение даёт инлайн-ошибку и не стирает ввод → валидный контакт даёт «Заявка принята» → обновление страницы сразу показывает «Заявка принята» (не пустую форму) → двойной клик по кнопке отправки создаёт ровно одну запись → горизонтальной прокрутки нет. `npm run check` проходит после каждого шага.
  - Не трогаем: реальную ссылку на Tribute (её ещё нет — `ctaUrl` не используется на пути), навигацию, экран «Заявки», текст блоков лендинга.
  - Важно: это временная заглушка до подключения Tribute, см. пометку в `PRODUCT.md` → V1.

## Позже

- [x] Опубликовать preview.
  - Сайт живёт на GitHub Pages: `https://klsnnkt.github.io/11-tablic-marketolog/` (может измениться, см. пункт про короткий адрес ниже).
- [ ] Провести тест на трёх людях.

## План после ресерча (2026-09-18) — публикация и доработки

Собрано по итогам: наблюдений пользователя + аудита кода + проверки живого сайта в браузере (консоль, сетевые запросы, вес страницы — ошибок не найдено, вес страницы ~3.34 МБ). Ничего из этого ещё не сделано — только зафиксировано, чтобы не потерять. Решение по Tribute отменилось: не подходит, вместо него эквайринг + касса от банка Точка (будет 2026-09-19).

### Быстрые правки (без внешних зависимостей, можно в любой момент)

- [x] Фон `html` не задан (задан только у `body`) — вероятная причина чёрных полосок и «плавания» влево-вправо при скролле на мобильном. Сделано 2026-09-17: фон добавлен на `html`, `overscroll-behavior-x: none`. Отдельно нашёлся и исправлен ещё один шов (isolation:isolate + filter:blur рендерился в Safari иначе) — убрана isolation, подсветка блоков 1-3 перенесена на сами карточки (`.block--glow-1/-2/-3`).
- [x] Нет Open Graph-картинки и favicon — сделано 2026-09-18: `public/images/favicon.png` (тот же значок, что на кнопке «Забрать набор») и `public/images/og-image.jpg` (hero-фото + заголовок в стиле лендинга), подключены в `index.html`.
- [x] Картинка `public/images/about.png` весила 1.5 МБ (остальные — 100–420 КБ) — сжата. Сделано 2026-09-18: у файла реально используемый альфа-канал (не флэт), поэтому JPEG не годился; чистое lossless-пересжатие PNG почти не помогло (1.55 МБ → 1.49 МБ). Конвертировали в `public/images/about.webp` (quality 90, то же разрешение 922×1152, с альфой) — 1.55 МБ → 112 КБ. Обновлена ссылка в `src/project.js` (`about.photo`). Проверено скриншотами на 390/1440px — фото и свечение `.block--glow` под карточкой не отличимы от исходника, `npm run check` проходит.
- [x] У картинок таблиц (`.table-card img`) не был зарезервирован размер (в отличие от hero/about, где уже есть `aspect-ratio`) — на медленном интернете вёрстка могла «прыгать» при подгрузке. Сделано 2026-09-18: все 5 скринов таблиц одного размера (1272×605), добавлены `width:100%; aspect-ratio:1272/605; object-fit:cover; display:block;` на `.table-card img` в `src/styles.css`. Проверено скриншотами на 390/1440px — вид карточек не изменился, `npm run check` проходит.
- [x] Экраны `#/workspace` и `#/styleguide` были открыты любому по прямой ссылке — не защищены, выглядели как случайно забытая админка/черновик. Сделано 2026-09-20: в `src/app.js` роутер `render()` редиректит на `/` только для `/workspace` и `/styleguide`, всё остальное (включая якорь `#order`) по-прежнему рендерит главную как раньше. Код самих экранов (`renderWorkspace`, `workspaceContent`, `renderStyleguide`, `store.js` и т.д.) не трогали — точечный фикс роутинга, а не удаление файлов.
  - **Важно, была регрессия и фикс:** первая версия правки блокировала не только `/workspace`/`/styleguide`, а вообще любой хэш кроме `/` — из-за этого сломались кнопка «Забрать набор» (блок «Что внутри») и свайп в hero: оба используют `location.hash = "order"` для прокрутки к блоку цены, и редирект на `/` эту прокрутку обнулял. Поймано при повторной проверке 2026-09-20 (просили ещё раз перепроверить Метрику — заодно прогнал полную регрессию по кнопкам) и в тот же день исправлено — редирект сузили строго до `/workspace` и `/styleguide`. Урок: при блокировке набора «плохих» путей в этом роутере — делать это явным списком запрещённых путей, а не белым списком разрешённых, т.к. на странице есть легитимные хэши-якоря (`#order`) не начинающиеся с `/`.
  - Проверено Playwright после фикса: `/#/workspace` и `/#/styleguide` превращаются в `/#/` и показывают hero-блок; клик по кнопке «Забрать набор» и по свайпу в hero снова даёт `location.hash === "#order"` и прокручивает к блоку цены; ошибок в консоли нет; `npm run check` проходит.
- [x] Шрифт Unbounded был подключён с серверов Google (`fonts.googleapis.com`) — Montserrat уже самохостился в `public/fonts/`, привели к одному подходу. Сделано 2026-09-20: скачали сам шрифт (это вариативный шрифт — один файл покрывает все начертания 700–900, Google просто отдавал один и тот же файл на все три веса) под кириллицу и латиницу (`public/fonts/unbounded-cyrillic.woff2`, `unbounded-latin.woff2` — вместе ~80 КБ, взяты только нужные наборы символов, т.к. сайт только на русском), добавили `@font-face` в `src/styles.css` с `font-weight: 700 900` и теми же `unicode-range`, что были у Google. Убрали `<link rel="preconnect">`/`<link rel="stylesheet">` на `fonts.googleapis.com`/`fonts.gstatic.com` из `index.html`. Проверено: Playwright показывает 0 запросов к Google, `getComputedStyle` заголовка отдаёт `Unbounded`, шрифт на скриншоте выглядит как раньше, `npm run check` проходит.
- [x] Подключить аналитику (Яндекс.Метрика) — сейчас не было способа увидеть, сколько людей доходит до кнопки оплаты. Сделано 2026-09-20: пользователь положил код счётчика (номер 112836115, с вебвизором/картой кликов) в проект, вставлен в начало `<head>` в `index.html` — так рекомендует сама Метрика, для точного замера времени загрузки. Проверено Playwright: `window.ym` определён, счётчик реально шлёт запросы на `mc.yandex.ru`, ошибок в консоли нет, сайт открывается как раньше, `npm run check` проходит.
- [~] ОТМЕНЕНО 2026-09-20. В репозитории публичный `LICENSE` (MIT, копирайт «AI Room Club» — автор стартового шаблона, не мы) при публичном репозитории — формально разрешает свободно копировать код. Пользователь решил не заниматься этим пунктом: обсудили риски (см. `DECISIONS.md` → «LICENSE — решили не трогать»), признали низкими и не критичными для бизнеса, приоритет отдали другим задачам.
- [~] ОТМЕНЕНО 2026-09-20. Сократить адрес сайта бесплатно — переименовать репозиторий в `klsnnkt.github.io` (это имя свободно, тогда адрес станет `https://klsnnkt.github.io/` без пути) либо хотя бы в более короткое имя. Пользователь решил оставить текущий адрес (`https://klsnnkt.github.io/11-tablic-marketolog/`) как есть.
- [x] Почистить `package.json` от следов исходного шаблона (`name: airc-vibe-page`, `homepage: focussam.github.io/...`). Сделано 2026-09-20: `name` → `11-tablic-marketolog`, `description` переписан под реальный проект, `repository`/`homepage` указывают на настоящие адрес репозитория и сайта. Поле `license` не трогали — отдельное решение про LICENSE (см. выше), его пока не меняем. Ничего в коде эти поля не читает (проверено `grep`). Проверено: `npm run check`, `npm run doctor`, `npm run build` проходят без ошибок.
- [x] Нет своей страницы 404 — по опечатке в адресе был виден стандартный экран GitHub, не сайт. Сделано 2026-09-20: `404.html` в корне проекта, в фирстиле — переиспользует уже существующие классы (`.panel.narrow`, `.cell-tag`, `.button`), новых стилей не добавляли. Подключает `src/styles.css` и favicon по абсолютным путям (`/11-tablic-marketolog/...`) — это важно именно для 404-страницы: GitHub Pages показывает её содержимое на любой ошибочной ссылке любой вложенности, а относительные пути (как в `index.html`) в таком случае резолвились бы неправильно. Добавлена одна строка в `scripts/build.mjs`, чтобы файл попадал в сборку (`dist`). Проверено: страница открывается, стили и шрифт применяются, ссылка «На главную» ведёт на реальный лендинг, `npm run check`/`doctor`/`build` проходят.
- [~] ОТМЕНЕНО 2026-09-20. `robots.txt`/`sitemap.xml` — низкий приоритет, трафик идёт из Telegram, а не из поиска. Пользователь решил пропустить.

### Telegram — можно включить уже сейчас, без кода

- [x] Создать в канале обычную (открытую, без «заявок на вступление») пригласительную ссылку — вступление сразу, без одобрения владельцем. Ссылку нигде публично не публиковать — она появится только на будущей странице «Спасибо за покупку», которую видят исключительно после оплаты. Сделано пользователем 2026-09-20: ссылка `https://t.me/+_8kKE2-aB4AwZWJi` уже создана и встроена в `thanks.html`, подтверждено — вступление мгновенное, без одобрения владельцем.
- [ ] Взять за привычку раз в 1–2 недели (или при подозрении на утечку) отзывать эту ссылку в настройках канала и создавать новую — лёгкая защита от пересылки ссылки третьим лицам, без автоматизации. При ротации не забыть обновить ссылку на странице «Спасибо». Договорённость с пользователем: он сам будет писать раз в 1–2 недели с новой ссылкой, чтобы обновить `thanks.html` — процесс уже запущен, ждём первого напоминания.

### Точка — реализуем многоразовую ссылку (без бэкенда, без CMS-виджета)

- [x] Получить от Точки понимание, как подключить оплату на самописном сайте. Сделано 2026-09-20: написали в поддержку Точки напрямую (сайт не на CMS, без своего сервера). Ответ поддержки: готового встраиваемого виджета для самописных сайтов у банка нет технически. Два варианта — (1) полноценная интеграция по API, требует свой сервер/бэкенд; (2) многоразовая ссылка на оплату (`Интернет-эквайринг → Создать заказ → Многоразовый`) — готовая ссылка без сервера. Выбрали (2), см. `DECISIONS.md` → «Способ подключения оплаты уточнён». Логин и «Секрет для подписи заказа» из раздела CMS-интеграций для этого способа не нужны — подтверждено поддержкой, что они актуальны только для готовых CMS/конструкторов.
- [x] Собрать статическую страницу «Спасибо за покупку»: текст + пригласительная ссылка на канал (готовится заранее, см. пункт выше — не зависит от Точки). Сделано 2026-09-20: `thanks.html` в корне проекта, та же схема, что и `404.html` (переиспользует `.panel.narrow`, `.cell-tag`, `.button` — новых стилей нет; ресурсы по абсолютным путям `/11-tablic-marketolog/...`, `noindex`, файл добавлен в `scripts/build.mjs`). Текст и кнопка — ровно по формулировке пользователя («Супер, платеж прошел!» + «ЗАБРАТЬ ТАБЛИЦЫ»), ссылка на канал `https://t.me/+_8kKE2-aB4AwZWJi` (пользователь один раз прислал не ту ссылку, поправили до коммита). Напоминание на будущее: пока это статическая страница без проверки оплаты (вариант A) — ссылка на канал лежит прямо в HTML, значит действует та же договорённость про ручную ротацию ссылки раз в 1–2 недели, что и в разделе «Telegram» ниже; при ротации обновлять и этот файл. Проверено: страница открывается, стили/шрифт/кнопка/ссылка корректны, `npm run check`/`build` проходят.
- [x] Настроить редирект на страницу «Спасибо» после успешной оплаты. Сделано 2026-09-20 пользователем: в личном кабинете Точки, раздел «Страницы после оплаты», указана ссылка `https://klsnnkt.github.io/11-tablic-marketolog/thanks.html` как страница успеха — после оплаты покупателя переносит туда автоматически.
- [x] Уточнить в личном кабинете Точки, есть ли отдельная страница/настройка для неуспешной оплаты. Сделано 2026-09-20: у Точки есть своя готовая страница ошибки оплаты — решили пока оставить её как есть, свою не делаем (см. `DECISIONS.md`).
- [x] Заполнить пустые «Состояния» в `FLOW.md` (Успех/Ошибка при оплате) — оба заполнены 2026-09-20.
- [x] Вставить многоразовую ссылку на сайт. Сделано 2026-09-20: `project.ctaUrl` в `src/project.js` заменён с заглушки `#` на реальную ссылку `https://tochkaplace.com/ia/1189ff73-20e4-4d73-b885-0443fd393b3c`. Поменяна механика кнопок в `src/app.js` — раньше свайп в hero и кнопка блока «Что внутри» только прокручивали к блоку цены (`#order`), теперь обе (плюс кнопка в самом блоке цены, она уже читала `ctaUrl`) сразу ведут на оплату: у свайпа `complete()` вместо `location.hash = "order"` делает `window.location.href = project.ctaUrl`, у кнопки блока 2 `href="#order"` заменён на `href="${project.ctaUrl}"`. Блок с ценой (`#order`) никуда не делся, просто перестал быть обязательной остановкой перед кнопками. Проверено Playwright: все 3 CTA (клик по пилюле hero, Enter на пилюле, кнопка блока «Что внутри», кнопка в блоке цены) ведут на ссылку Точки; скриншоты 390/1440px — вид не изменился; ошибок в консоли нет; `npm run check` проходит.
- [x] Проверить, нужна ли реальная база — не нужна, подтверждено поддержкой Точки: многоразовая ссылка не требует бэкенда, чек и связь с оплатой ведёт сама Точка, сайт остаётся статической витриной.
