# TASKS.md

## Сейчас

- [x] Единая точка для ссылки «Забрать набор»
  - Пользователь: все кнопки ведут в одно и то же место (пока заглушка) вместо разрозненных `href="#"`.
  - Затронуто: `src/project.js` (новое поле `ctaUrl`), `src/app.js` (кнопки читают его вместо хардкода).
  - Проверка руками: открыть сайт, нажать все кнопки «Забрать набор» по очереди — ведут на один и тот же адрес; значение меняется в одном месте в `project.js`.
  - Не трогаем: саму ссылку на Tribute (её ещё нет), шапку/навигацию, страницу «Заявки», текст лендинга.
  - Проверено: в коде найдено 3 кнопки (в задаче было указано 5) — все читают `project.ctaUrl`, автоматически проверено скриншотами в браузере (десктоп и мобильная ширина), `npm run check` проходит.

## Позже

- [ ] Решить, нужна ли реальная база.
- [ ] Опубликовать preview.
- [ ] Провести тест на трёх людях.
