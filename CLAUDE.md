# Инструкции Claude Code

— Рабочий протокол — в `AGENTS.md`, прочитай его первым.
— После каждого шага статус: сделал / проверил / осталось / риск.
— Перед удалением файлов, публикацией, сменой прав — спроси подтверждение.
— После изменений обновляй `AGENTS.md`/`CLAUDE.md` и другие файлы, которые ИИ читает при каждом запросе.
